# 採点基準

プログラミング問題の採点基準（採点方法）について記述しています。

- 各課題（問題）1問につき、100点満点（`SCORE_BASE`, `SCORE_CLEAR`, `SCORE_TIME`の合計で100点）
- `SCORE_BASE`は、その問題のテストケースの正解数に応じた点数
- `SCORE_CLEAR`は、すべてのテストケースに合格した場合のボーナス点数
- `SCORE_TIME`は、学生がその問題を開始してからの経過時間で計算される点数（開始から3分経過することに、30点から1点ずつ減点されていく。90分以上の場合は、0点）
    - 教員が設定した課題の開始時間では無く、学生がその問題にアクセスして「開始」した時間から数える
- プログラムの制約を満たしていない場合は、`SCORE_TOTAL`から減点
    - 下記のメモの値を参照

## 配点メモ

Python形式で記述

```python
### 基本的な点数配分（TechFULの採点基準をもとに算出）
# テストケースの正解数に応じた点数（テストケースが5問ある場合は、1問10点で合計50点。テストケースが3問の場合は、1問あたり50/3点で合計50点になるように自動計算）
SCORE_BASE  = 50
# 全てのテストケースに合格した場合のボーナス点
SCORE_CLEAR = 20
# 提出までに要した時間に応じた点数（開始から3分経過することに、1点ずつ減点）
SCORE_TIME  = 30
# 全ての要素を合計した点数（1問当たり100点満点を想定）
SCORE_TOTAL = SCORE_BASE + SCORE_CLEAR + SCORE_TIME

### 条件に応じた減点の値
# コンパイル時に警告が出た場合の減点
DEDUCT_WARN = 20
# 変数の数に対する制約（条件を満たしてなければこの点数が減点）
DEDUCT_VAR  = 20
# 配列の数に対する制約（条件を満たしてなければこの点数が減点）
DEDUCT_ARR  = 20
# ポインタの数に対する制約（条件を満たしてなければこの点数が減点）
DEDUCT_PTY  = 20
# 反復処理（for, while）の数に対する制約（この条件を満たしていなければこの点数が減点）
DEDUCT_LOOP = 10
# if文の数に対する制約（条件を満たしていなければこの点数が減点）
DEDUCT_IF   = 10
```
